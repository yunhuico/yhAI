define(['app'], function(app) {
	'use strict';
	app.controllerProvider.register('InformationController', ['$scope', '$q', function($scope, $q) {
    function init() {
      // do nothing
    }

    init();
  }]);
})